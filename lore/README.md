# lore/

Empty skeleton. Holds accumulated, evidence-backed claims from completed benchmark runs — shared prior state a future run can start an entrant from (see ARCHITECTURE.md's definition of LORE). Every entry here must resolve to a record in `evidence/`; a LORE entry with no evidence reference is a bug in the harness, not a valid state.
