# experiments/

Empty skeleton. Holds run configurations and logs for actual benchmark executions once the `chromatic-universe-v1` harness exists — not narrative writeups. See `benchmarks/chromatic-universe-v1/README.md` for the loop this directory will record instances of.
